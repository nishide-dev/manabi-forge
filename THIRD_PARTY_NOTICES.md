# Third-Party Notices

このリポジトリに含まれる、または依存するサードパーティ成果物の一覧。教材ごとの帰属は各教材ディレクトリの `ATTRIBUTION.md` に記録する。

## テンプレート

- [react-template](https://github.com/nishide-dev/react-template) — `web/` の初期構成の由来。

## 依存関係

- Web: `web/package.json` および `pnpm-lock.yaml` を参照。
- Python: `python/pyproject.toml` および `python/uv.lock` を参照。

## 今後の追加

フォント、TeX パッケージ、公的データセットなどを利用する際は、本ファイルに出典とライセンスを追記すること(spec §20.1)。
